# LabWiFiMon Linux Probe — Python dependencies
# Install with:  pip install -r requirements.txt
# Or via install.sh which creates an isolated venv.

# MQTT client — used by probe.py, monitor_mode.py
paho-mqtt>=1.6.1,<3

# YAML config parsing
pyyaml>=6.0

# HTTP throughput measurement
requests>=2.28.0

# ── Optional ──────────────────────────────────────────────────────────────────
# scapy is only needed for monitor_mode.py (frame capture).
# Uncomment the line below, or install separately:
#   pip install scapy
# Requires libpcap-dev (apt) or libpcap-devel (dnf) at compile time.
#
# scapy>=2.5.0
